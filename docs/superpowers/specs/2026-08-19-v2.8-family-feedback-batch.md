# planner-v2.8 — family feedback batch (2026-08-19 night)

User feedback session 2026-08-19 evening; every item below was explicitly
confirmed (defaults accepted). Build overnight, verify on live before morning.

## 1. Subjects tab: explicit order + Miscellaneous

Fixed order (subjects.js): `singapore, loe, geography, core-ruhamah, core-hala,
core-quran, core-art, science, jj, history, core-mama`. Unknown ids sort after,
by the old status order. Rationale: top three are what Nabila needs daily.
`core-mama` is DISPLAY-renamed to **"Miscellaneous"** via the activity's `name`
field (data write in the migration, NOT a code change — the Week-grid category
label "Mama Classes" stays, those blocks are her teaching time; only the
Subjects card renames).

## 2. LoE: current period starts at 101; D is 121–160

She finished lessons 81–100 before tracking began. The family wants "the
current period and how much is left":
- `loe-c` → `{firstUnit: 101, lastUnit: 120, done: 2}` (101, 102 done).
- `loe-d` → `{firstUnit: 121, lastUnit: 160, done: 0}` (publisher-confirmed
  40 lessons — user verified).
- LoE totals everywhere become **2/60 lessons**.
- MIGRATION (live-data script, run after deploy): apply the two chain edits;
  remap existing `loe-c` log rows' `session` index (old numbering counted from
  81: new = old − 20; any old row with session < 20 is pre-tracking — keep the
  row, stamp it an explicit `label: "Lesson <81+old>"` field so history views
  never mislabel it, and null its `curriculum` linkage is NOT allowed — keep
  curriculum, views must prefer `e.label` when present); append today's log row
  for lesson 102 `{date: '2026-08-19', activityId: 'loe', status: 'done',
  curriculum: 'loe-c', session: 1}` (verified: no LoE row exists today, live
  done was 21 = through 101).
- Timeline bands for LoE become **5-lesson bands** (101-105 … 156-160): new
  optional `chain.bandSize` (validated positive int in sanitizePlan, default
  `BAND_SIZE`=10), set to 5 on both loe chains in the migration. Band keys
  change ⇒ the existing LoE baseline rows orphan ⇒ migration RE-FREEZES the
  LoE baseline (setBaseline semantics, as-of migration day) after the chain
  edits. Singapore's Sept-1 baseline is untouched.

## 3. Today tab: a ticked lesson keeps its own name

Today, ticking the LoE row flips its label from "Lesson 102" to "Lesson 103"
immediately. Confirmed wrong. When a paced activity has a `done` log row for
TODAY, the Today row shows the label of the session that was COMPLETED
(derived from the log row's `curriculum` + `session` — prefer the row's
explicit `label` field when present, see §2) with its ✓; the "next" label only
appears when nothing is logged today (i.e. tomorrow morning). The Subjects tab
keeps showing next (correct today, unchanged). Un-ticking restores the next
label (identity via the same log row, existing togglePaced semantics).

## 4. Subjects card: lessons-based header + per-chapter dot grid

Replace the paced card's `0/251 · next: …` line and single progress bar:
- Header line: **"2/60 lessons · 3% · next: Ch 1 · Lesson 1 · textbook"** —
  lesson-based counts (a tb-wb lesson = its 2 sessions together; a half-done
  lesson counts 0.5; chapter Reviews are EXCLUDED from the lesson count and %).
  New pure helpers in js/plan/model.js (additive only): `lessonTotals(act)` →
  `{done, total}` in lesson units across the chain (tb-wb: lessons per chain;
  simple: units; tests excluded; done from sessions→lessons at 0.5 grain).
- **Singapore (tb-wb chains): dot grid** — one cluster per chapter, one dot
  per lesson (123 dots), chapter-colored from a rotating 15-color palette
  (CSS vars .dgc0…; distinct hues, both themes). Half-done lesson = half-fill
  (CSS half via linear-gradient). Chapter Reviews render as a small distinct
  ◆ marker at the end of their chapter's cluster (filled when done).
- **LoE (simple chains): 5-lesson mini-bars** — one small bar per bandSize
  lessons (12 bars for 101–160), filling proportionally; same palette
  rotation; complete band = fully filled.
- Both live inside the card above the 📅 Timeline toggle; existing .sbar is
  REPLACED for paced-with-chain cards only (others unchanged). Seamless with
  existing card design (sline typography, 420px column, phone-first: dots wrap
  within clusters, never sideways-scroll).

## 5. Note line → computed pace line

For ACTIVE paced activities with a chain: replace the static `a.note` render
with a computed line: **"▲ 2 wks ahead of plan · 4 sessions this week ·
5-day streak"** — (a) whole-book delta = last unfinished sessioned timeline
row's now vs baseline (same chip math as the Timeline; "≈ on plan" / ▲ / ▼;
omitted when no baseline), (b) `done` log-row count for this activity Mon–Sun
this week, (c) `dailyStreak` (existing helper). Static `a.note` still renders
for non-paced/non-active cards. The migration CLEARS the stale notes on
`singapore` and `loe` (and geography's after titles load, §Geography).

## 6. Year tab: per-subject session history drill-down

Per subject row on the Year view, one tap expands a collapsible history
(`<details>` pattern):
- Log rows for that subject (activityId or its template events' eventIds, plus
  Telegram one-offs attributed to it), **newest first**, grouped by month with
  headers "This month" / "Last month" / "June 2026" style.
- Each row: `Tue Aug 19 · 3:30–4:30 ✓` — time range shown for timed rows
  (template slot times via eventId; override start/end for one-offs); paced
  rows show the session label instead of times: `Tue Aug 19 · Lesson 102 ✓`
  (prefer row.label, else sessionLabel(chain, row.session)). Status marks
  ✓/◐/✗ as elsewhere.
- Useful for paced = the lesson labels (user emphasized).
- Print untouched (Year never prints). Day-view parity rule N/A (not a grid
  visual). esc() everywhere.

## 7. Release & verification

- All model.js changes additive (lessonTotals, bandSize passthrough) — bot
  parity untouched (bot never calls these).
- New unit tests: lessonTotals (halves, tests-excluded, mixed chains),
  bandSize=5 band keys, Today completed-label logic, order comparator, note
  pace-line pieces, migration invariants (row remap, label stamping).
- `?v=2026-08-20-1` on all five stamps · print harness (plan.css changes) ·
  bare `node --test` green · tag `planner-v2.8` · deploy · then the §2/§1/§5
  data migration + LoE re-baseline · live verification incl. phone-width.

## Geography (data-only, no code)

Extract the 36 weekly unit titles from ~/Downloads/"BYL Level 0_ Around the
World 2023.pdf" → `geo-1.titles = {1: "...", …, 36: "..."}` (unitWord stays
"Week"), clear its stale note, status stays **planned** (family activates
later by word).
